#pragma once

class app_manager
{
private:
    int consoleWidth;
    int consoleHeight;
    int radius;      // ?????? ????? (length)
    int velocity;    // ???????? ? ??????? (1..30)
    int frequency;   // ????? ? ??????? (1..30)
    bool epilepsy;   // true - ????????? ???? ??? ??????? ?????, false - ???????
    int explosionChance;     // 1..1000
    int minExplosionRadius;  // 1..10
    int maxExplosionRadius;  // 1..10

public:
    void getArguments();
    int generateX(); // ????????? X-????? ??? ????? 
    void startMove();
    app_manager(char* frequency, char* velocity, char* radius, char* explosionChance, char* minExplosionRadius, char* maxExplosionRadius, char* epilepsy);
    app_manager();
};
